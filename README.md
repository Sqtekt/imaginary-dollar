# IMD Headquarters

Static public-facing project homepage for Imaginary Dollar.

Open `index.html` locally or publish the `site/` directory with any static web host.

Before publishing a live network dashboard, connect it only to a read-only public API and never expose wallet private keys.
